#include "DataFormats/L1TMuon/interface/EMTFHit.h"

#include <cassert>
#include <type_traits>

// static_assert(std::is_trivially_copyable<l1t::EMTFHit>::value, "EMTFHit is not trivially copyable");

namespace l1t {
} // End namespace l1t
